spring:
  application:
    name: flight-api
  datasource:
    url: jdbc:mysql://localhost:3306/flight_db
    username: root
    password: 811024
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  file:
    name: flightapi.log
    path: logs